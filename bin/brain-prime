#!/usr/bin/env php
<?php

require_once __DIR__ . "/../vendor/autoload.php";

use function BrainGamesProject\Cli\showGreeting;
use function BrainGamesProject\Games\Prime\getPrimeGameCondition;
use function BrainGamesProject\Engine\showGameCondition;
use function BrainGamesProject\Games\Prime\createPrimeGameData;
use function BrainGamesProject\Engine\checkAnswer;
use function BrainGamesProject\Engine\congratulate;

$name = showGreeting();
showGameCondition(getPrimeGameCondition());
$userCorrectAnswers = 0;
for ($i = $userCorrectAnswers; $i < SUCCESS; $i++) {
    if (checkAnswer(createPrimeGameData()) === true) {
        continue;
    } else {
        return;
    }
}
congratulate();